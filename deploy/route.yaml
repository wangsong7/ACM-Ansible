# route.yaml
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    openshift.io/host.generated: "true"
    #argocd.argoproj.io/sync-wave: "1"
  creationTimestamp: null
  labels:
    app: hello-song
    app.kubernetes.io/instance: hello-song    
  name: hello-song
spec:  
  port:
    targetPort: 8080
  to:
    kind: Service
    name: hello-song
    weight: 100
status:
  ingress:
    - conditions:
        - status: 'True'
          type: Admitted
